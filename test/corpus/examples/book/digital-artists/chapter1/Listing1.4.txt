==========
Listing1.4
==========

// Listing 1.4 Defining and using an integer variable

// declare an integer
int myPitch;

// store a value in it
220 => myPitch;

// print it out
<<< myPitch >>>;


---

(source_file
  (comment)
  (comment)
  (statement
    (variable_declaration
      (primitive_type)
      (variable_identifier)))
  (comment)
  (statement
    (chuck_operation
      (int)
      (chuck_operator)
      (variable_identifier)))
  (comment)
  (statement
    (debug_print
      (variable_identifier))))
