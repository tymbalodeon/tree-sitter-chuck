10, 20 => int foo;
(10), 20 => int foo;
10, (20 => int foo);
(10, 20 => int foo);
// (10, 20) => Foo foo;

// (one + two).three();
// (a + b).num();
// (a + b).one.two();
// (a + b).num.array();

// (1 + 2).num.two.three;
